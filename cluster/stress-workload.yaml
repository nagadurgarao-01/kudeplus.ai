apiVersion: v1
kind: Pod
metadata:
  name: cpu-stress
  namespace: default
spec:
  containers:
  - name: stress
    image: polinux/stress
    args: ['stress', '--cpu', '2', '--vm', '1', '--vm-bytes', '256M']
    resources:
      limits:
        cpu: '2'
        memory: '512Mi'
